using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class isDefenderFalling : MonoBehaviour
{/*
    public string defenderName;
    private PlayerController playerController;
    private DestroyOutOfBounds destroyOutOfBounds;
    public bool defenderIsFalling = false;
    private GameObject egoist;
    public bool done = false;


    void OnDisable()
    {
        if (destroyOutOfBounds.ResetDefender)
        {
            defenderIsFalling = false;            
        }
    }

    void Awake()
    {
        egoist = GameObject.Find("egoist");
        playerController = egoist.GetComponent<PlayerController>();
        destroyOutOfBounds = gameObject.GetComponent<DestroyOutOfBounds>();        
    }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (defenderName == "defender right" && playerController.rightDfalling)
        {
            defenderIsFalling = true;

            if (done)
            {
                playerController.rightDfalling = false;
                done = false;
            }
        }

        if (defenderName == "left defender" && playerController.leftDfalling)
        {
            defenderIsFalling = true;

            if (done)
            {
                playerController.leftDfalling = false;
                done = false;
            }
        }
    }*/
}
